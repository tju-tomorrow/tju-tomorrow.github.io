/* global NexT, CONFIG */

var Affix = {
  init: function() {},
  checkPosition: function() {}
};

NexT.utils.getAffixParam = function() {
  return { top: 0, bottom: 0 };
};

document.addEventListener('DOMContentLoaded', () => {
  // Claude layout: no floating affix / banner injection
});
